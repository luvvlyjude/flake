{
  hm.programs.satty = {
    enable = true;

    settings = {
      general = {
        disable-notifications = true;
        default-hide-toolbars = true;
        initial-tool = "brush";
        actions-on-escape = [
          "save-to-file"
          "exit"
        ];
      };
    };
  };
}
